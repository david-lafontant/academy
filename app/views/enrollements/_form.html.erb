<%= form_with(model: enrollement) do |form| %>
  <% if enrollement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(enrollement.errors.count, "error") %> prohibited this enrollement from being saved:</h2>

      <ul>
        <% enrollement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.text_field :course_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
